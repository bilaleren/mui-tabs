{
  "name": "@mui-tabs/utils",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "test": "echo \"Test not found.\"",
    "build": "yarn build:node && yarn build:modern && yarn build:types",
    "build:node": "node ../../scripts/build.mjs --env node --out-dir ./utils",
    "build:modern": "node ../../scripts/build.mjs --env modern --out-dir ./utils/esm",
    "build:types": "node ../../scripts/buildTypes.mjs --out-dir ./utils && node ../../scripts/buildTypes.mjs --out-dir ./utils/esm"
  }
}
